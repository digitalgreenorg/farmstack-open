version: '3'
services:

  # The core platform, mounts docker control socket and route definition into the image
  consumer-core:
    image: farmstack/trusted-connector
    tty: true
    stdin_open: true
    volumes:
      - ./logs/provider/:/root/log/
      - /var/run/docker.sock:/var/run/docker.sock
      - ../../fs-assets/deploy/allow-all-flows.pl:/root/deploy/allow-all-flows.pl
      - ../../fs-assets/etc/settings.mapdb:/root/etc/settings.mapdb
      - ../../fs-assets/sample-certificates/consumer-keystore.p12:/root/etc/consumer-keystore.p12
      - ../../fs-assets/sample-certificates/truststore.p12:/root/etc/truststore.p12
#      - ./consumer-routes/farmstack-farmos-data-consumer.xml:/root/deploy/farmstack-farmos-data-consumer.xml
      - ./consumer-routes/farmstack-farmos-file-consumer.xml:/root/deploy/farmstack-farmos-file-consumer.xml

    ports:
      - 9000:9000
      - 9001:9001 # For jetty data routes
      - 9002:9002 # For jetty file routes
    # Using two networks ids-wide and provider-internal for external comunication and internal communication respectively
    networks:
      - ids-wide
      - farmos-consumer-internal

  farmos-consumer:
    image: farmstack/farmos-consumer:restapi
    ports:
      - 5003:5003
    networks:
      - farmos-consumer-internal

networks:
  ids-wide:
    driver: bridge
  farmos-consumer-internal:
    driver: bridge